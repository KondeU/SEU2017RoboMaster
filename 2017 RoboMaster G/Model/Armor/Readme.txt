Use V4L2 to adjust the camera exposure, so need "sudo apt-get install libv4l-dev".

Sometimes apt-get is not a http link (https), use "sudo apt-get sudo apt-get install apt-transport-https" to fix it.
